#!/bin/bash

THIS_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )

if [ -z "$DATABASE_URL" ]; then
  DATABASE_URL="postgres://localhost/open_analytics_development"
fi

eval `$THIS_DIR/_parse_db_params.rb $DATABASE_URL`

psql $DATABASE_NAME < ${THIS_DIR}/../schema.sql
